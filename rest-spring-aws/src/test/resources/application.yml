server:
  port: 8888

spring:
  application:
    name: rest-spring-aws
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: none
    show-sql: false

springdoc:
  pathsToMatch: /api/**/v1/**
  swagger-ui:
    use-root-path: true